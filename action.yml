name: 'clang-format-checker'
description: 'Google-style linter, wip.'
author: 'aMahanna'
branding:
  icon: 'code'
  color: 'orange'
inputs: 
  sources:
    description: Names of the files/directories to check (space-delimited)
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'